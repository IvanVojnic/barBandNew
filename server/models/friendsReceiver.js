/*import { Sequelize } from 'sequelize';
import sequelize from '../utils/database.js';

const FriendsReceiver = sequelize.define('friendsReceiver' , {
    userSender: {
        type : Sequelize.INTEGER,
        allowNull: false
    },
    userReceiver: {
        type : Sequelize.INTEGER,
        primaryKey: true,
        autoIncrement: false,
        allowNull: false
    },
    status: {
        type : Sequelize.STRING,
        allowNull: false
    }
})

export default FriendsReceiver*/
