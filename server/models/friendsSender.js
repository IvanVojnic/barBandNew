/*import { Sequelize } from 'sequelize';
import sequelize from '../utils/database.js';

const FriendsSender = sequelize.define('friendsSender' , {
    userSender: {
        type : Sequelize.INTEGER,
        primaryKey: true,
        autoIncrement: false,
        allowNull: false
    },
    userReceiver: {
        type : Sequelize.INTEGER,
        allowNull: false
    },
    status: {
        type : Sequelize.STRING,
        allowNull: false
    }
})

export default FriendsSender*/
